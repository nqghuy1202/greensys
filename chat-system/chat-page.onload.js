/* CHAT HỆ THỐNG — Execute when Page Loads
 * Paste vào: Page → JavaScript → "Execute when Page Loads".
 *
 * Toàn bộ hàm + state đã nằm ở "Function and Global Variable Declaration"
 * (chat-page.fgvd.js). CHAT_AUS_ID cũng đã set ở đó. Ô này chỉ còn init.
 */
window.csInit();
